<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TGQuran" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/M.Green</item>
        <item name="android:statusBarColor">@color/M.Green</item>
    </style>

    <style name="Theme.TGQuran" parent="Base.Theme.TGQuran" />

    <style name="bottomNavigationStyle" parent="ShapeAppearance.Material3.NavigationBarView.ActiveIndicator">

    </style>
    <style name="textview" parent="TextAppearance.AppCompat">
        <item name="android:background">@drawable/bg_shape_hadeeth</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_margin">10dp</item>
        <item name="android:textSize">30sp</item>
        <item name="android:fontFamily">@font/quraan</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>